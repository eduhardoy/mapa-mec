import React from "react";
import styled from "styled-components";

const DistanceWrapper = styled.div``;

const DistanceTool = () => {
  return <DistanceWrapper></DistanceWrapper>;
};

export default DistanceTool;
